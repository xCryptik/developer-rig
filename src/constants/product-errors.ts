// Display name validation errors
export const NAME_EMPTY: string = 'Name must not be empty';
export const NAME_CHAR_LIMIT: string = 'Name must be less than 256 characters';

// SKU validation errors
export const SKU_EMPTY: string = 'SKU must not be empty';
export const SKU_CHAR_LIMIT: string = 'SKU must be less than 256 characters';
export const SKU_WHITESPACE: string = 'SKU must not contain any whitespace';
export const SKU_UNIQUE: string = 'SKU must be unique';

// Amount validation errors
export const AMOUNT_EMPTY: string = 'Amount must not be empty';
export const AMOUNT_OUT_OF_RANGE: string = 'Amount must be between 1 and 10,000';